# MeuProjeto - Template Full Stack

Template orientado por Markdown para projetos C#/.NET + React.

Comece por `CRIAR-PROJETO.md`. As regras ficam em `tasks/rules`, as receitas reutilizáveis em `tasks/skills` e cada alteração concreta em `tasks/specs/changes`.
