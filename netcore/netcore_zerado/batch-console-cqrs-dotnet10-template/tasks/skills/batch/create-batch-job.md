# Skill - create batch job

Siga as regras em `tasks/rules` e a Spec ativa. Gere somente o necessário para esta responsabilidade, mantendo CQRS, DI, CancellationToken, logging e testabilidade. Não introduza mensageria ou componentes fora do escopo.
