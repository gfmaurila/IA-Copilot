# CLAUDE.md — Node.js

Este projeto/template já possui estrutura e decisões existentes.

Leia:
1. `PROJECT.md`
2. `PROJECT-STATE.md`
3. `AI-WORKFLOW.md`
4. README/documentação existente
5. `package.json`, lockfile, código e testes
6. task solicitada

Execute SOMENTE a task solicitada.

Não recrie scaffold, não troque package manager/framework/ORM e não faça refatorações globais fora do escopo. Preserve contratos, persistência, testes e Docker existentes.

Ao final, rode as validações aplicáveis e reporte PASS/PARTIAL/FAIL.
