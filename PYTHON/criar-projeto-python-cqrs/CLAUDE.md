# CLAUDE.md — Python + CQRS

Este projeto/template já possui estrutura e decisões existentes.

Leia:
1. `PROJECT.md`
2. `PROJECT-STATE.md`
3. `AI-WORKFLOW.md`
4. README/documentação existente
5. configuração Python, dependências, código e testes
6. task solicitada

Execute SOMENTE a task solicitada.

Não recrie scaffold, não substitua CQRS, não troque framework/ORM/gerenciador de dependências e não faça refatoração global fora do escopo. Preserve Commands, Queries, handlers, persistência, migrations, testes e Docker existentes.

Ao final, rode as validações aplicáveis e reporte PASS/PARTIAL/FAIL.
