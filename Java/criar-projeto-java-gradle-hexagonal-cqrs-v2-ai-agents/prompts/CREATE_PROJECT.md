# Prompt — Create Project

Crie/complete o projeto seguindo `AGENTS.md` e `orchestration/workflows/PROJECT_CREATION.md`.

Não implemente antes de gerar Requirements, Architecture Plan e Execution Plan.
Use as Skills existentes em `tasks/skills`.
Respeite rigorosamente Hexagonal + Ports & Adapters + CQRS.
Execute os Quality Gates e corrija falhas antes de concluir.
