# Java + Gradle + Spring Boot + CQRS + MySQL

Template orientado a:

- Java 25 LTS
- Spring Boot 4.1.x
- Gradle 9.7.x / Kotlin DSL
- CQRS
- Domain Events
- Spring Data JPA
- MySQL
- Flyway
- Spring Security + JWT
- JUnit 5
- Testcontainers
- React Admin/Site
- Rules + Skills + Specs + Copilot

As versões devem ser revalidadas no momento da execução para utilizar releases estáveis e compatíveis.


## AI Agents — v2

Esta versão adiciona orquestração por agentes:

`Requirements -> Architect -> Tech Lead -> Developer -> Tester -> Reviewer -> Documentation`

Antes do código são gerados `REQUIREMENTS.md`, `ARCHITECTURE_PLAN.md` e `EXECUTION_PLAN.md`. A entrega passa por 8 Quality Gates e só é finalizada com build, testes, review e documentação aprovados.
